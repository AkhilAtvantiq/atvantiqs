import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",
  // images: {
  //   unoptimized: true,
  // },
  // trailingSlash: true,
  // basePath: "/atvantiqs",
  /* config options here */
};

export default nextConfig;
